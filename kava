#!/bin/bash

source $HOME/.config/kava/config

getCover () {
    cover=$(get-cover "$(mpc current -f %artist%)" "$(mpc current -f %album%)")
    cd "$mpdpath"/"$dir"
    if [[ $cover == "Fetch error." ]]; then
        echo "...could not download cover for \""$(mpc current -f '%artist% - %album%')"\", setting placeholder image..."
        convert -resize "$size" "$noimage" /tmp/cover.png
        convert -size "$res" "$image" -page "$coordinates" /tmp/cover.png -layers flatten "$wallpaper"
        feh --bg-scale "$wallpaper"
    else
        echo "...found cover, setting wallpaper"
        wget "$cover" -O cover>/dev/null 2>&1
    fi
    if [[ $(file --mime-type cover | awk '{ print $NF }') == "image/png" ]]; then
        mv cover cover.png
        cp cover.png /tmp/cover.png
        mogrify -resize "$size" /tmp/cover.png
        convert -size "$res" "$image" -page "$coordinates" /tmp/cover.png -layers flatten "$wallpaper"
        feh --bg-scale "$wallpaper"
        rm /tmp/cover.png
    elif [[ $(file --mime-type cover | awk '{ print $NF }') == "image/jpeg" ]]; then
        mv cover cover.jpg
        cp cover.jpg /tmp/cover.jpg
        mogrify -resize "$size" /tmp/cover.jpg
        convert -size "$res" "$image" -page "$coordinates" /tmp/cover.jpg -layers flatten "$wallpaper"
        feh --bg-scale "$wallpaper"
        rm /tmp/cover.jpg
    fi
}


main () {
    while true; do
        current="$(mpc current)"
        mpc idle player>/dev/null 2>&1
    sleep 1

    if [[ -z $(mpc current) ]]; then
        convert -resize "$size" "$noimage" /tmp/cover.png
        convert -size "$res" "$image" -page "$coordinates" /tmp/cover.png -layers flatten "$wallpaper"
        feh --bg-scale "$wallpaper"
    elif [[ $(mpc current) == "$current" ]]; then
        :
    else
        dir="$(dirname "$(mpc current --format %file%)")"
        cd "$mpdpath"/"$dir"
        if [[ -a cover.png ]]; then
            echo "Cover for \""$(mpc current -f '%artist% - %album%')"\" found, setting wallpaper..."
            cp cover.png /tmp/cover.png
            mogrify -resize "$size" /tmp/cover.png
            convert -size "$res" "$image" -page "$coordinates" /tmp/cover.png -layers flatten "$wallpaper"
            feh --bg-scale "$wallpaper"
            rm /tmp/cover.png
        elif [[ -a folder.jpg ]]; then
            echo "Cover for \""$(mpc current -f '%artist% - %album%')"\" found, setting wallpaper..."
            mv folder.jpg cover.jpg
            cp cover.jpg /tmp/cover.jpg
            mogrify -resize "$size" /tmp/cover.jpg
            convert -size "$res" "$image" -page "$coordinates" /tmp/cover.jpg -layers flatten "$wallpaper"
            feh --bg-scale "$wallpaper"
            rm /tmp/cover.jpg
        elif [[ -a cover.jpg ]]; then
            echo "Cover for \""$(mpc current -f '%artist% - %album%')"\" found, setting wallpaper..."
            cp cover.jpg /tmp/cover.jpg
            mogrify -resize "$size" /tmp/cover.jpg
            convert -size "$res" "$image" -page "$coordinates" /tmp/cover.jpg -layers flatten "$wallpaper"
            feh --bg-scale "$wallpaper"
            rm /tmp/cover.jpg
        else
            echo "No cover found for \""$(mpc current -f '%artist% - %album%')"\", trying to download..."
            getCover
        fi
    fi
    done
}

reset () {
    if [[ -z $(mpc current) ]]; then
        convert -size "$res" "$image" -page "$coordinates" "$noimage" -layers "$wallpaper"
        feh --bg-scale "$wallpaper"
    else
        dir="$(dirname "$(mpc current --format %file%)")"
        cd "$mpdpath"/"$dir"
        mv folder.jpg cover.jpg>/dev/null 2>&1
        mv folder.png cover.png>/dev/null 2>&1
        mv cover.png cover.png.bak>/dev/null 2>&1
        mv cover.jpg cover.jpg.bak>/dev/null 2>&1
        getCover
        if [[ -a cover.png ]]; then
            rm -f cover.png.bak
        else
            rename .png.bak '.png' *.bak
        fi
        if [[ -a cover.jpg ]]; then
            rm -f cover.jpg.bak
        else
            rename .jpg.bak '.jpg' *.bak
        fi
    fi
}

if [[ $1 == reset ]]; then
    reset
else
    main
fi
